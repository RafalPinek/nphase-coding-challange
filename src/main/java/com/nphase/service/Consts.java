package com.nphase.service;

import java.math.BigDecimal;

final class Consts {

    private Consts() {}

    static final int AMOUNT_THRESHOLD = 3;

    static final BigDecimal DISCOUNT = BigDecimal.valueOf(10);

    static final BigDecimal ONE_HUNDRED = BigDecimal.valueOf(100);
}
